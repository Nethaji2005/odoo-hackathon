// App.jsx is intentionally minimal.
// All routing and layout is handled by AppRoutes.
// Member 4 (Dashboard) can add their navigation/layout shell here.
export default function App() {
  return null;
}
